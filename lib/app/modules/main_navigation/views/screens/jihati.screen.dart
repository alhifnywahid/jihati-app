import 'package:flutter/material.dart';

class JihatiScreen extends StatelessWidget {
  const JihatiScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('JihatiScreen')),
      body: Center(child: Center(child: Text("Jihati"))),
    );
  }
}
